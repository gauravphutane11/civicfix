import type { PriorityFactor } from "../../types";
export default function PriorityReceipt({total,factors}:{total:number;factors:PriorityFactor[]}){
 return <div className="font-mono text-[0.78rem] bg-paper-raised text-paper-ink border border-paper-ink/15 rounded-sm p-4 relative"><div className="absolute -top-2 left-4 bg-paper-raised px-2 text-[0.6rem] tracking-widest uppercase text-paper-ink/50">Priority reasoning</div><div className="flex justify-between items-baseline pb-2 mb-2 border-b border-dashed border-paper-ink/25"><span className="font-semibold">Why this score</span><span className="text-paper-ink/50 text-[0.68rem]">/ 100 pts</span></div><ul className="space-y-2.5">{factors.map(f=><li key={f.key}><div className="flex justify-between items-baseline gap-3"><span>{f.label}</span><span className="whitespace-nowrap">{f.points>=0?"+":""}{f.points}<span className="text-paper-ink/40">/{f.max_points}</span></span></div><p className="text-paper-ink/55 text-[0.68rem] leading-snug mt-0.5">{f.explanation}</p></li>)}</ul><div className="flex justify-between items-baseline mt-3 pt-2 border-t border-dashed border-paper-ink/25 font-semibold"><span>Total</span><span>{total} / 100</span></div></div>;
}
